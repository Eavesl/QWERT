package com.bawei.liuqi20191209.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.bawei.liuqi20191209.bean.UriBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "URI_BEAN".
*/
public class UriBeanDao extends AbstractDao<UriBean, Void> {

    public static final String TABLENAME = "URI_BEAN";

    /**
     * Properties of entity UriBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Json = new Property(0, String.class, "json", false, "JSON");
    }


    public UriBeanDao(DaoConfig config) {
        super(config);
    }
    
    public UriBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"URI_BEAN\" (" + //
                "\"JSON\" TEXT);"); // 0: json
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"URI_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, UriBean entity) {
        stmt.clearBindings();
 
        String json = entity.getJson();
        if (json != null) {
            stmt.bindString(1, json);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, UriBean entity) {
        stmt.clearBindings();
 
        String json = entity.getJson();
        if (json != null) {
            stmt.bindString(1, json);
        }
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public UriBean readEntity(Cursor cursor, int offset) {
        UriBean entity = new UriBean( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0) // json
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, UriBean entity, int offset) {
        entity.setJson(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(UriBean entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(UriBean entity) {
        return null;
    }

    @Override
    public boolean hasKey(UriBean entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
